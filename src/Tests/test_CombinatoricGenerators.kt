/**
 * Created by andrej on 23.8.2017
 */


/*
import CombinatoricGenerators.combinations
import CombinatoricGenerators.combinationsWithReplacement
import CombinatoricGenerators.permutations
import CombinatoricGenerators.permutationsWithReplacement

fun main(args: Array<String>) {
    val xs = permutationsWithReplacement("abc", 3)
    xs.map { println(it) }

    println()

    val ys = permutations("abcd")
    ys.map { println(it) }

    println()

    val zs = combinations("abcd", 2)
    zs.map { println(it) }

    println()

    val ws = combinationsWithReplacement("abcd", 2)
    ws.map { println(it) }
}
*/